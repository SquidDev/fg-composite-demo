pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

includeBuild("composite-basic")
includeBuild("composite-subproject")
include("subproject")
